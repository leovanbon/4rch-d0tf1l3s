[general]
# Set the location-provider: 'geoclue2', 'manual'
# We use manual to avoid tracking/network requests
location-provider=manual

# Set the screen temperature (Day/Night)
temp-day=4000
temp-night=4000

# Optional: Fade duration in seconds when changing
fade=1

[manual]
# Example coordinates for Paris, France. 
# REPLACE THESE with your own city's coordinates.
lat=48.85
lon=2.35
